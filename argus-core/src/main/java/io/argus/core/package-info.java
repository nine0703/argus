/**
 * ARGUS Core
 * <p>
 * Defines the foundational abstractions and semantic contracts for the ARGUS agent runtime.
 * This module contains no implementations and no framework dependencies.
 * <p>
 * The core is designed to be:
 * <ul>
 *   <li>Auditable: all agent behaviors can be observed and replayed</li>
 *   <li>Deterministic: execution steps are explicit and traceable</li>
 *   <li>Extensible: implementations are provided by higher-level modules</li>
 * </ul>
 *
 * <p>
 * ARGUS Core 定义了 ARGUS 代理运行时的基础抽象与语义契约。
 * 该模块不包含任何实现，也不依赖任何框架。
 *
 * <p>
 * Core 的设计目标：
 * <ul>
 *   <li>可审计：所有代理行为都可被观测与回放</li>
 *   <li>可推理：执行步骤是显式且可追踪的</li>
 *   <li>可扩展：具体实现由上层模块提供</li>
 * </ul>
 */
package io.argus.core;