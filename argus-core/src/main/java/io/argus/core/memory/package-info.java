/**
 * Agent memory abstractions.
 *
 * <p>
 * This package defines the contract for agent memory systems.
 * Memory is treated as an append-only, auditable store of facts.
 *
 * <p>
 * The core does not prescribe how memory is stored or indexed.
 *
 * <p>
 * Agent 记忆抽象。
 *
 * <p>
 * 本包定义了代理记忆系统的基础契约。
 * 记忆被视为一种可审计、可回放的事实存储。
 *
 * <p>
 * Core 不规定记忆的具体存储或索引方式。
 */
package io.argus.core.memory;