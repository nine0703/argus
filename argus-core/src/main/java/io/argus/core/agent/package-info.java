/**
 * Agent domain abstractions.
 *
 * <p>
 * This package defines what an Agent is, how it is driven, and how its execution
 * progresses through explicit decision loops.
 *
 * <p>
 * An Agent itself contains no execution logic.
 * All behaviors are driven by an {@link io.argus.core.agent.AgentLoop}.
 *
 * <p>
 * Agent 模型定义。
 *
 * <p>
 * 本包定义了什么是 Agent、Agent 的上下文，以及代理是如何通过
 * 显式的决策循环被驱动的。
 *
 * <p>
 * Agent 本身不包含执行逻辑，
 * 所有行为均由 {@code AgentLoop} 驱动。
 */
package io.argus.core.agent;